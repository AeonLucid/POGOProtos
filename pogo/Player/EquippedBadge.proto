syntax = "proto3";
package POGOLib.Pokemon.Proto.Player;

import public "Enums/HoloBadgeType.proto";

message EquippedBadge {
  POGOLib.Pokemon.Proto.Enums.HoloBadgeType badge_type = 1; // enum HoloBadgeType
  int32 level = 2;
  int64 next_equip_change_allowed_timestamp_ms = 3;
}
