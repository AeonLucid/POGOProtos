syntax = "proto3";
package POGOProtos.Networking.Requests.Messages;

message SetFavoritePokemonMessage {
	fixed64 pokemon_id = 1; // uint64 does not work; see issue #110; verified that all other places use fixed64 for pokemon_id
	bool is_favorite = 2;
}
